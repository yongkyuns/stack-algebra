on: [push, pull_request]

name: Build

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  queue-release:
    name: Focused validation handoff
    runs-on: ubuntu-latest
    steps:
      - run: echo "Focused issue #10 validation owns this temporary branch head."
